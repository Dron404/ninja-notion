import React from "react";
import styles from "./CoverChange.module.scss";

export const CoverChange = (): React.ReactElement => {
  return (
    <>
      <div className={styles.cover__button}>Change cover</div>
    </>
  );
};
